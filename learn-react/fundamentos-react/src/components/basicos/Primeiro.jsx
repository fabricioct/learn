import React from "react";

export default function Primeiro() {
  
  const msg = 'Exemplo com Function como default  export';

    return (
    <div>
       <h2>Primeiro Componente</h2>
       <p>{msg}</p>
    </div>
  );

}